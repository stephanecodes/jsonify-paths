{
  "name": "jsonify-paths",
  "version": "0.1.0",
  "description": "Convert an array of paths to a JSON object",
  "main": "index.js",
  "scripts": {
    "test": "xo --fix && ava --verbose"
  },
  "author": "Stephane Janicaud (https://stephanecodes.github.io)",
  "license": "MIT",
  "devDependencies": {
    "ava": "^1.0.1",
    "xo": "^0.23.0"
  }
}
